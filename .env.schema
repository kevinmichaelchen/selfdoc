# Cloud TTS keys for the Voice panel, declared varlock-style: this schema is
# committed (it holds no values); actual keys come from `varlock run -- pnpm
# dev` or plain exported env vars. See docs/adr/0002-api-key-handling.md.
# @defaultSensitive=false @defaultRequired=false
# ---

# ElevenLabs key for synthetic narration
# @sensitive
ELEVENLABS_API_KEY=

# Fish Audio key for synthetic narration
# @sensitive
FISH_API_KEY=
